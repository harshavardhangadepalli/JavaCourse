public class App4 {
    public static void main(String[] args) {
        // you can use +=, -=, /=, and *=
        // some operators are : >, <, ==, !=, >=, <=, 
        // logical : $$, ||, !
        // bitwise : &, |, !
        // ternary operator
        boolean a, flag = true;
        a = flag ? true : false;
        // can also be like
        a = (flag == true) ? true : false;
        System.out.println(a);
        // used to assign 1 of 2 values
    }
}
